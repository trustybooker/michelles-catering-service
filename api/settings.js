import { neon } from '@neondatabase/serverless';
import { createHmac, timingSafeEqual } from 'node:crypto';
const sql = () => neon(process.env.DATABASE_URL);
const valid = (req) => { const token = String(req.headers.cookie || '').match(/michelle_admin=([^;]+)/)?.[1]; if (!token || !process.env.ADMIN_SESSION_SECRET) return false; const [email, expiry, sig] = token.split('.'); const payload = `${email}.${expiry}`; const expected = createHmac('sha256', process.env.ADMIN_SESSION_SECRET).update(payload).digest('hex'); return email === process.env.ADMIN_EMAIL && Number(expiry) > Date.now() && sig && timingSafeEqual(Buffer.from(sig), Buffer.from(expected)); };
export default async function handler(req, res) { res.setHeader('Cache-Control','no-store'); if (!valid(req)) return res.status(401).json({ error:'Owner sign-in required' }); const db = sql(); if (req.method === 'GET') { const rows = await db`SELECT settings FROM site_settings WHERE id=1`; return res.status(200).json(rows[0]?.settings || {}); } if (req.method === 'PUT') { const settings = req.body && typeof req.body === 'object' ? req.body : {}; const clean = JSON.stringify(settings).slice(0,20000); await db`INSERT INTO site_settings (id,settings,updated_at) VALUES (1,${clean}::jsonb,now()) ON CONFLICT (id) DO UPDATE SET settings=excluded.settings,updated_at=now()`; return res.status(200).json({ ok:true }); } res.setHeader('Allow','GET, PUT'); return res.status(405).json({ error:'Method not allowed' }); }
