import { createHmac } from 'node:crypto';
export default async function handler(req,res) { if(req.method!=='POST') return res.status(405).json({error:'Method not allowed'}); const email=String(req.body?.email||'').trim().toLowerCase(); if(!email || email!==String(process.env.ADMIN_EMAIL||'').toLowerCase()) return res.status(200).json({ok:true}); if(!process.env.RESEND_API_KEY||!process.env.RESEND_FROM_EMAIL||!process.env.ADMIN_SESSION_SECRET) return res.status(503).json({error:'Owner email is not configured'}); const expiry=Date.now()+15*60*1000; const payload=`${email}.${expiry}`; const sig=createHmac('sha256',process.env.ADMIN_SESSION_SECRET).update(payload).digest('hex'); const link=`${process.env.PUBLIC_SITE_URL||'https://michelles-catering-service.vercel.app'}/admin.html?owner_token=${encodeURIComponent(`${payload}.${sig}`)}`; const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${process.env.RESEND_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({from:process.env.RESEND_FROM_EMAIL,to:[email],subject:"Michelle's Catering owner sign-in",html:`<p>Use this secure link to open the Michelle's Catering owner dashboard:</p><p><a href="${link}">Open owner dashboard</a></p><p>This link expires in 15 minutes.</p>`})}); if(!response.ok) return res.status(502).json({error:'Email provider rejected the request'}); return res.status(200).json({ok:true}); }
