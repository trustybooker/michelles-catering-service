import { neon } from '@neondatabase/serverless';
export default async function handler(req,res) { if(req.method!=='GET') return res.status(405).json({error:'Method not allowed'}); try { const rows=await neon(process.env.DATABASE_URL)`SELECT settings FROM site_settings WHERE id=1`; const settings=rows[0]?.settings||{}; const allowed=['businessName','tagline','heroMessage','phone','serviceArea','zelle','cashapp','paymentNote','instagram','facebook','tiktok','classicsPrice','celebrationPrice','officePrice','sweetsPrice']; return res.status(200).json(Object.fromEntries(allowed.filter(k=>k in settings).map(k=>[k,settings[k]]))); } catch { return res.status(503).json({error:'Settings unavailable'}); } }
