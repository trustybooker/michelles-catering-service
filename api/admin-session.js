import { createHmac, timingSafeEqual } from 'node:crypto';
export default async function handler(req,res) { const token=String(req.query?.token||''); const [email,expiry,sig]=token.split('.'); if(!email||!expiry||!sig||email!==process.env.ADMIN_EMAIL||Number(expiry)<Date.now()||!process.env.ADMIN_SESSION_SECRET) return res.status(401).json({error:'Invalid or expired owner link'}); const payload=`${email}.${expiry}`; const expected=createHmac('sha256',process.env.ADMIN_SESSION_SECRET).update(payload).digest('hex'); if(!timingSafeEqual(Buffer.from(sig),Buffer.from(expected))) return res.status(401).json({error:'Invalid owner link'}); res.setHeader('Set-Cookie',`michelle_admin=${token}; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=604800`); return res.status(200).json({ok:true}); }
